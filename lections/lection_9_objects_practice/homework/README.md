# Изучить материал


# Лекция

Создать объект data в котором будет свойство list. 
data.list - массив.
Создать методы:
+ fillArray(length)
+ renderArray()
+ setMarker(positionX, marker, id) // marker => &
+ moveMarker(id, newX)